import { component, createShowcaseDemoRegistry } from "@/showcase/create-demo"

export const calendarShowcaseDemoRegistry = createShowcaseDemoRegistry([
  component("calendar", "Calendar", "calendar", "Single month calendar surface for date picker and scheduling flows."),
])
